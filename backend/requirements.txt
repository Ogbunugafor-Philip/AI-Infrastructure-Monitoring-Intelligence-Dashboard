# AI Infrastructure Monitoring — backend dependencies (locked).
# Versions pinned to what was resolved & installed into backend/venv.
# Regenerate the unpinned source list from requirements.in.

# --- Web framework / ASGI ---
fastapi==0.136.3
uvicorn[standard]==0.48.0
python-multipart==0.0.29

# --- Database / ORM / migrations ---
sqlalchemy[asyncio]==2.0.50
asyncpg==0.31.0
alembic==1.18.4

# --- Configuration ---
python-decouple==3.8
pydantic[email]==2.13.4
pydantic-settings==2.14.1

# --- Authentication & cryptography ---
passlib[argon2]==1.7.4
argon2-cffi==25.1.0
python-jose[cryptography]==3.5.0
cryptography==48.0.0

# --- Rate limiting ---
slowapi==0.1.9

# --- Email (async SMTP) ---
aiosmtplib==5.1.0

# --- Background tasks / scheduling / cache ---
celery[redis]==5.6.3
redis==6.4.0
apscheduler==3.11.2

# --- SSH / remote server access ---
paramiko==5.0.0

# --- Security auditing ---
pip-audit==2.10.0
